{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "launch",
      "type": "codelldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/bake",
      "args": ["-p", "resources/tests/valid", "foo:"],
      "preLaunchTask": "cargo-build",
      "initCommands": ["command source ~/.lldbinit"]
    }
  ]
}
