name: foo
recipes:
  post-test:
    run: |
      echo "Hello Foo Post Test!"
    dependencies:
      - test

  build:
    inputs:
      - build.sh
    outputs:
      - target
    run: |
      ./build.sh

  test:
    run: |
      echo "Hello Foo Test!"
    dependencies:
      - build
