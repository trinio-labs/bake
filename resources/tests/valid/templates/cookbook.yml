name: "templates"
description: "Test cookbook using templates"
recipes:
  build:
    template: "build-template"
    parameters:
      language: "typescript"
      build_command: "echo 'Build completed successfully!'"
  
  test:
    template: "test-template"
    parameters:
      test_command: "echo npm run test:ci"
      coverage: true
  
  # Recipe using template but overriding the run command
  test-override:
    template: "test-template"
    run: "echo 'This run command overrides the template'"
    parameters:
      test_command: "echo npm run test:unit"
      coverage: false
  
  # Regular recipe without template
  deploy:
    description: "Deploy the application"
    run: |
      echo "Deploying application..."
      echo "Deployment complete!"
    dependencies: ["build", "test"]
